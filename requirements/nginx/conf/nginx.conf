server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name yoti.42.fr;
    ssl_certificate /etc/nginx/ssl/yoti.crt;  # Changed from yoti.42.fr.crt
    ssl_certificate_key /etc/nginx/ssl/yoti.key;  # Changed from yoti.42.fr.key
    ssl_protocols TLSv1.2 TLSv1.3;

    root /var/www/html;
    index index.php index.html;

    location /static/ {
        proxy_pass http://static-page:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass wordpress:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ ^/adminer(/.*$|$) {
        proxy_pass http://adminer:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name portainer.yoti.42.fr;

    ssl_certificate /etc/nginx/ssl/yoti.crt;  # Changed from yoti.42.fr.crt
    ssl_certificate_key /etc/nginx/ssl/yoti.key;  # Changed from yoti.42.fr.key
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://portainer:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}